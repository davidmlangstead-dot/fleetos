export type FormField = { key: string; label: string; type: "TEXT" | "TEXTAREA" | "NUMBER" | "DATE" | "CHECKBOX" | "SELECT"; required: boolean; options?: string[] };
export type JobType = { id: string; name: string; trade: string; description: string | null; colour: string; defaultPriority: string; defaultDurationMinutes: number; workflow: string[]; formSchema: FormField[]; requiredSkills: string[]; riskAssessmentRequired: boolean; customerSignatureRequired: boolean; isSystem: boolean; isActive: boolean };
export type PersonOption = { id: string; firstName: string; lastName: string; email: string | null; personType: string; accessRole: string; skills: string[] };
export type VehicleOption = { id: string; registration: string; type: string; status: string };
export type CustomerOption = { id: string; name: string; accountReference: string | null; email: string | null; phone: string | null };
export type SiteOption = { id: string; customerId: string; name: string; address: string; postcode: string | null; contactName: string | null; contactPhone: string | null; contactEmail: string | null; accessNotes: string | null };
export type AssetOption = { id: string; siteId: string; name: string; assetType: string; assetReference: string | null; manufacturer: string | null; model: string | null; serialNumber: string | null; location: string | null };
export type JobConfig = { jobTypes: JobType[]; people: PersonOption[]; vehicles: VehicleOption[]; customers: CustomerOption[]; sites: SiteOption[]; assets: AssetOption[] };
export type JobRow = { id: string; reference: string; title: string; status: string; priority: string; source: string; scheduledStart: string | null; scheduledEnd: string | null; dueAt: string | null; createdAt: string; jobTypeName: string | null; trade: string | null; colour: string | null; customerName: string; siteName: string | null; siteAddress: string | null; sitePostcode: string | null; assetName: string | null; registration: string | null; assignments: Array<{ id: string; name: string; type: string }>; actualCostPence: number; sellPence: number; quotePence: number | null; purchaseOrderNumber: string | null };
