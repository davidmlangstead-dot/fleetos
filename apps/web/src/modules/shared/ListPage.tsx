import { Search } from "lucide-react";
export function ListPage({ title, description, action }: { title: string; description: string; action: string }) { return <section className="page"><div className="page-heading"><div><p className="eyebrow">Fleet operations</p><h1>{title}</h1><p className="subtle">{description}</p></div><button className="primary-button">+ {action}</button></div><section className="panel empty-panel"><div className="search"><Search size={19}/><input placeholder={`Search ${title.toLowerCase()}…`}/></div><div className="empty-state"><h2>{title}, without the clutter</h2><p>This module is ready for its first live records. Its API boundary and data model are already in place.</p><button className="primary-button">+ {action}</button></div></section></section>; }
