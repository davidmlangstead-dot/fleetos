{
  "version": 2,
  "buildCommand": "mkdir -p apps/web/dist && cp pnpm-lock.yaml apps/web/dist/pnpm-lock.yaml && printf '<!doctype html><title>Prisma lockfile capture</title>' > apps/web/dist/index.html",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "outputDirectory": "apps/web/dist"
}
